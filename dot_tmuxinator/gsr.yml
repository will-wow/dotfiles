name: gsr
root: ~/repos/gsr
socket_path: /tmp/gsr
windows:
  - servers:
      layout: even-horizontal
      panes:
        - yarn ws sdk start
        - yarn ws indexer start
        - 
  - workspace:
      layout: even-horizontal
      panes:
        - yarn ws contracts start
        - 
