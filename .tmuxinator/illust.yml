name: illust
root: ~/repos/illust-marketplace
socket_path: /tmp/illust
windows:
  - servers:
      layout: even-horizontal
      panes:
        - yarn start:api
        - yarn start:marketplace
        - yarn start:webar --https
        - yarn start:scraper
  - workspace:
      layout: even-horizontal
      panes:
        - 
        -
  - pops:
      layout: even-horizontal
      panes:
        - cd ../illust-contracts && yarn blockchain
        - cd ../illust-contracts && yarn hardhat run scripts/deployPop.ts --network localhost
  - ngrok:
      layout: even-horizontal
      panes:
        - yarn ngrok http https://illust.localhost:3001 --region=us --hostname=willwow.ngrok.io
        -
