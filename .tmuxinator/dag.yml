name: dag
root: ~/repos/agora

windows:
  - main:
      layout: even-horizontal
      panes:
        - cd elixir && iex -S mix
        - cd elixir && git status
  - servers:
      layout: even-horizontal
      panes:
        - cd elixir && MIX_ENV=demo mix phx.server
        - cd front-react && yarn start
        - cd trading-view && http-server -p 9090
